import { Box, styled } from "@mui/material";

const ProgressContainer = styled(Box)({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  width: "100vw",
  height: "60vh",
});

const S = {
  ProgressContainer,
};
export default S;
